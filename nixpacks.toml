[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = ["pip install -r web-app/backend/requirements-full.txt"]

[start]
cmd = "cd web-app/backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
